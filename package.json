{
  "name": "complyhub-ui-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run",
    "allure:report_test": "allure generate allure-results --clean -o allure-report",
    "test": "npm run cy:run || npm run posttest",
    "posttest": "npm run allure:report_test",
    "browser:chrome": "npx cypress run --browser=chrome --headed --reporter mocha-allure-reporter",
    "allure:report": "allure generate allure-results --clean -o allure-report && allure open  allure-report",
    "allure:clear": "if exist allure-results rmdir /q /s allure-results && if exist allure-report rmdir /q /s allure-report && if exist cypress\\screenshots rmdir /q /s cypress\\screenshots && if exist cypress\\videos rmdir /q /s cypress\\videos",
    "testcases:tag": "npx cypress run --headed --env grepTags=@smokeTag --reporter mocha-allure-reporter"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cypress/grep": "^4.0.1",
    "@shelex/cypress-allure-plugin": "^2.40.2",
    "allure-commandline": "^2.29.0",
    "cypress": "^13.6.2",
    "mocha-allure-reporter": "^1.4.0"
  }
}
